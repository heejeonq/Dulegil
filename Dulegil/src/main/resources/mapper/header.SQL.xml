<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="header">

<select id="alarmCnt" parameterType="hashmap" resultType="Integer">
SELECT COUNT(*)
FROM BULLETIN_BOARD B INNER JOIN ACCOMPANY_APPLY A
ON B.POST_NO = A.POST_NO
WHERE B.MEMBER_NO = #{memNo}
</select>

<select id="AANm" parameterType="hashmap" resultType="hashmap">
SELECT M.NM AS MNM
FROM MEMBER M INNER JOIN 
(SELECT A.MEMBER_NO
FROM BULLETIN_BOARD B INNER JOIN ACCOMPANY_APPLY A
ON B.POST_NO = A.POST_NO
WHERE B.MEMBER_NO = #{memNo})BA
ON M.MEMBER_NO = BA.MEMBER_NO
</select>


</mapper>
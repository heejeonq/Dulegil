<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mypage">


	<insert id="insertMember" parameterType="hashmap">
		INSERT INTO MEMBER(MEMBER_NO, EMAIL, AUTHORITY_NO, PWD, NM, PHONE_NO, DATE_BIRTH, GENDER, ZIP_CODE, ADDRESS, DETAIL_ADDRESS )
		VALUES(MEMBER_SEQ.NEXTVAL, #{email}, 2, #{pwd}, #{nm}, #{phn}, #{dtBrt}, #{gen}, #{zcd}, #{adr}, #{adrDtl})
	</insert>

	<select id="getMyinfo" resultType="hashmap" parameterType="hashmap">
		SELECT MEMBER_NO, EMAIL, NM, PHONE_NO, TO_CHAR(DATE_BIRTH, 'YYYY.MM.DD') AS DATE_BIRTH, GENDER, ZIP_CODE, ADDRESS, DETAIL_ADDRESS, IMG_FILE
		FROM MEMBER
		WHERE MEMBER_NO = #{memNo}
	</select>

		<select id="checkPwd" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_NO, NM
		FROM MEMBER
		WHERE DEL_DT IS NULL 
		AND AUTHORITY_NO = 2
		AND PWD = #{loginPwd}
	</select>

</mapper>